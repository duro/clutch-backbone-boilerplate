{
  "name": "<%= appName %>",
  "version": "<%= version %>",
  "author": "<%= realname %>",
  "description": "<%= appDescription %>",

  "buildDir": "../../../public/statics/apps",

  "devDependencies": {
    "grunt": "~0.4.1",
    "bbb": "~0.2.0",
    "grunt-contrib-jshint": "~0.6.0",
    "grunt-contrib-copy": "~0.4.1",
    "grunt-contrib-clean": "~0.5.0",
    "grunt-contrib-watch": "~0.5.3"
  },

  "peerDepenencies": {
    "grunt-cli": "*",
    "bower": "*"
  },

  "scripts": {
    "postinstall": "bower install -s"
  }
}